// import { exec } from 'child_process';
// const{ logger } =(await import('./utils/logger'));

// const command = '';

// exec(command, (error, stdout, stderr) => {
//   if (error) {
//     logger.error(`Error executing command: ${error.message}`);
//     return;
//   }
//   logger.success(`Command output: ${stdout}`);
// });
